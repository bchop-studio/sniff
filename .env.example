# Copy to `.env` and fill in real values. Never commit `.env`.

# Future use: optional LLM-as-judge detection (out of scope for v0.1).
# Keep placeholder structure so future agents see the shape.
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
