"""Test fixtures: real-world-shaped text samples for sniff to chew on.

These exist as files on disk so the CLI can demo against the same inputs
the tests use (`sniff tests/fixtures/clean_email.txt`).
"""
